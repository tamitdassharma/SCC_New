projection;
strict ( 1 );
use draft;
use side effects;

define behavior for /ESRCC/C_UploadStaging alias UploadStaging
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}